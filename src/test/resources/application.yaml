spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/qafordevs_test
    username: qafordevs_user
    password: QAFORDEVS123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
